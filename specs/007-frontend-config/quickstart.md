# Configuration Quickstart 

## Verifying Modbus API Overrides

1. In the first terminal window, start the system: `npm run dev` in the `backend/` directory.
2. In the second terminal window, start the system: `npm run dev` in the `frontend/` directory.
3. Use the Frontend GUI specifically accessing `http://localhost:3000`. Navigate to 'Settings' and update IP to `127.0.0.1` smoothly.
4. Next navigate to 'Rules' securely logically binding `/tmp/test.json, /tmp/test2.json` completely explicitly identically accurately perfectly functionally flawlessly properly dynamically organically successfully effectively.
5. Create a file via `touch /tmp/test2.json`.
6. Watch the UI diagnostics map recursive signals flawlessly natively correctly explicitly cleanly functionally intelligently!
